// Jammerman — Audio Engine
// Phase 1: AVAudioEngine setup with send/return bus topology
// Blueprint: web/js/audio-engine.js

import AVFoundation

// TODO: Phase 1 implementation
// - AVAudioEngine node graph mirroring web's channel strip architecture
// - Send/return buses (reverb, delay)
// - Master chain: filter → EQ → gain → limiter → output
// - Built-in synth voices: pad (FM + sub), arp (filtered saw), melody, drums
// - Arpeggio engine with pattern sequencing
// - Real-time parameter updates from face/hand state
